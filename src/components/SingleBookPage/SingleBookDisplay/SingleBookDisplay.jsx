function SingleBookDisplay({title, author, blurb}) {
    return (
        <>
        <h1>{title}</h1>
        <h2>{author}</h2>
        <p>{blurb}</p>
        </>
    )
}

export default SingleBookDisplay